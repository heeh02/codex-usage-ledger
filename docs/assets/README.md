# Public assets

Every asset in this directory must be generated from synthetic demo data and
reviewed at full resolution before commit. Do not commit screenshots captured
from a real Codex ledger, even when private labels appear cropped or blurred.

`macos-dashboard-demo.png` is the README product screenshot. Regenerate it from
the Web mock client whenever its visible contract or fixture names change.
